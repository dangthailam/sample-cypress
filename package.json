{
  "name": "playplay",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "node ./cypress/cypress_runner --mode open --env-working production"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^9.5.1",
    "yargs": "^17.4.0"
  },
  "devDependencies": {
    "cucumber-html-reporter": "^5.5.0",
    "cypress-cucumber-preprocessor": "^4.3.1"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  }
}
